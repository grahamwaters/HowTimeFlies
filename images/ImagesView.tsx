import React from 'react';
import './images.scss';

import {IImagesViewProps} from './ImagesContainer';

const ImagesView = (props: IImagesViewProps) => {
   const { } = props

   return (
   <div className='images'>

   </div>
   )
} 

ImagesView.defaultProps = {

} 

export default ImagesView;
